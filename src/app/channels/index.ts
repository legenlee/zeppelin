import { InstallerChannel } from './installerChannel';
import { MetadataChannel } from './metadataChannel';

export const channels = {
  metadataChannel: MetadataChannel,
  installerChannel: InstallerChannel,
};
